// Converted-customer detail: same record as the lead page, but reached from
// the Customers list — this route keeps "Customers" highlighted in the
// sidebar and the back button pointing at Customers instead of Leads.
export { default } from "../../leads/[id]/page";
